extends base.pug

block content
  .container
    include nav.pug
    #blog
      h1#home-heading Blog Posts
      .separator
      .posts
        ul
          // up to 5 posts, but no more!
          each blogpost in collections.blogposts
            include mixins.pug
            li #[+tile(blogpost)]